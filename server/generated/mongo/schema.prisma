generator client {
  provider = "prisma-client-js"
  output   = "../../generated/mongo"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model Quest {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  title String
  desc  String
}
